## Local copy of .env example for development
SUPABASE_URL=
SUPABASE_KEY=
GEMINI_API_KEY=
RAPIDAPI_KEY=
EXECUTE_API_TOKEN=
PORT=5000
LOG_LEVEL=debug

## Frontend (Vite)
VITE_SENTRY_DSN=
# ===== REQUIRED KEYS (App won't work without these) =====

# Supabase Database
# Get from: https://supabase.com → Create project → Settings → API
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Gemini API (AI Chat/Fix Code)
# Get from: https://console.cloud.google.com → APIs → Generative Language API → Create API Key
VITE_GEMINI_API_KEY=your_gemini_api_key

# ===== OPTIONAL KEYS (Features work without these, but with limitations) =====

# Judge0 Self-Hosted (for code execution)
# Get from: Run 'docker-compose up -d' in project root
# Leave empty to use Piston (free public API) instead
VITE_JUDGE0_URL=http://localhost:2358

# Judge0 RapidAPI (fallback for code execution)
# Get from: https://rapidapi.com → Search "Judge0" → Subscribe → Copy X-RapidAPI-Key
# Only needed if self-hosted Judge0 isn't available
VITE_RAPIDAPI_KEY=your_rapidapi_key_here

# OpenAI (AI Fallback - uses Gemini first, then falls back to OpenAI)
# Get from: https://platform.openai.com → API Keys → Create new secret key
# Cost: Paid (~$0.01-0.10 per request)
VITE_OPENAI_API_KEY=your_openai_api_key

# Razorpay (Payments - for premium subscriptions)
# Get from: https://razorpay.com → Settings → API Keys
VITE_RAZORPAY_KEY_ID=rzp_live_your_key_id_here
VITE_RAZORPAY_KEY_SECRET=your_key_secret_here_keep_private
VITE_RAZORPAY_PLAN_ID=plan_Gr4RVS4t4k2q9e
VITE_RAZORPAY_WEBHOOK_SECRET=your_webhook_secret_here

# API Server URL (if using custom backend)
VITE_API_URL=http://localhost:5000
